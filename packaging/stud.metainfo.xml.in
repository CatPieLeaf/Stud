<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>@STUD_APP_ID@</id>
  <launchable type="desktop-id">@STUD_APP_ID@.desktop</launchable>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>AGPL-3.0-or-later</project_license>

  <name>Stud</name>
  <summary>Play Roblox on Linux</summary>

  <description>
    <p>
      Stud runs the real Roblox Android client on a Linux desktop. It is not
      a reimplementation and not an emulator: it loads Roblox's own engine
      and drives it through the same interfaces an Android phone would,
      supplying the parts a phone normally provides — the framework, the
      window, the input devices and the graphics stack.
    </p>
    <p>
      It keeps the vendor graphics driver out of the process that runs the
      engine, which is the crash class that makes other approaches unstable
      on Linux, and it renders through Vulkan with OpenGL fallbacks for
      older hardware.
    </p>
    <p>
      Stud does not include Roblox. You supply the Android application
      package yourself, and Roblox remains subject to its own terms.
    </p>
  </description>

  <categories>
    <category>Game</category>
  </categories>

  <keywords>
    <keyword>Roblox</keyword>
    <keyword>games</keyword>
  </keywords>

  <!--
    Screenshots are fetched over the network by every store that shows
    them, so they have to be URLs: a path to a file inside this package
    is not something Discover or GNOME Software can read. They are served
    from this repository itself, which means they resolve once main is
    pushed; the files are in packaging/screenshots/.

    The first one is the default: it is what a store lists the
    application by.
  -->
  <screenshots>
    <screenshot type="default">
      <caption>The Roblox home screen, running on a Linux desktop</caption>
      <image type="source" width="1013" height="783">https://raw.githubusercontent.com/CatPieLeaf/Stud/main/packaging/screenshots/home.png</image>
    </screenshot>
    <screenshot>
      <caption>An experience in progress</caption>
      <image type="source" width="1013" height="783">https://raw.githubusercontent.com/CatPieLeaf/Stud/main/packaging/screenshots/game1.png</image>
    </screenshot>
    <screenshot>
      <caption>An experience in progress</caption>
      <image type="source" width="1013" height="783">https://raw.githubusercontent.com/CatPieLeaf/Stud/main/packaging/screenshots/game2.png</image>
    </screenshot>
    <screenshot>
      <caption>An experience in progress</caption>
      <image type="source" width="1013" height="783">https://raw.githubusercontent.com/CatPieLeaf/Stud/main/packaging/screenshots/game3.png</image>
    </screenshot>
  </screenshots>

  <url type="homepage">https://github.com/CatPieLeaf/Stud</url>
  <url type="bugtracker">https://github.com/CatPieLeaf/Stud/issues</url>

  <developer id="io.github.catpieleaf">
    <name>CatPieLeaf</name>
  </developer>

  <supports>
    <control>keyboard</control>
    <control>pointing</control>
  </supports>

  <requires>
    <display_length compare="ge">768</display_length>
  </requires>

  <content_rating type="oars-1.1">
    <content_attribute id="social-chat">intense</content_attribute>
    <content_attribute id="social-audio">intense</content_attribute>
    <content_attribute id="social-info">moderate</content_attribute>
    <content_attribute id="money-purchasing">intense</content_attribute>
    <content_attribute id="violence-cartoon">moderate</content_attribute>
  </content_rating>

  <releases>
    <release version="@STUD_VERSION@" date="2026-09-13">
      <description>
        <p>The mouse cursor now behaves as it does on the desktop client.</p>
        <ul>
          <li>The cursor stays put through a camera rotation and follows the
              hand everywhere else, decided from the engine's own mouse
              state rather than guessed at.</li>
          <li>The pointer is kept inside the window during a camera drag, so
              a spin is no longer stopped by the edge of the screen.</li>
          <li>Input reaches the engine the moment the compositor delivers
              it, which also lifted the frame rate well past 60.</li>
          <li>The app shell's Exit button closes Stud.</li>
        </ul>
      </description>
    </release>
    <release version="0.1.0" date="2026-09-12">
      <description>
        <p>First packaged release.</p>
      </description>
    </release>
  </releases>
</component>
