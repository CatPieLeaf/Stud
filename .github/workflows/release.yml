# Builds Stud from source and attaches the packages to a release.
#
# It runs when a release is PUBLISHED, and at no other time: no push
# builds, no pull-request builds. (workflow_dispatch is here so the
# pipeline can be exercised without cutting a release -- it only ever
# runs when someone presses the button.)
#
# The long pole is ANGLE, which builds from source and takes about an
# hour. It is cached, so that happens once per container image and every
# later release restores it in seconds. The NDK and the extracted bionic
# are cached the same way. Caching works because tools/setup.sh skips any
# dependency that is already present -- a cache hit makes it a no-op.

name: Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to build and upload to"
        required: true

permissions:
  contents: write
  # ghcr.io, for the OCI image cpak installs from.
  packages: write

concurrency:
  group: release-${{ github.event.release.tag_name || inputs.tag }}
  cancel-in-progress: false

env:
  # The version the packages carry comes from the tag being released, so
  # an rpm can never disagree with the release it is attached to. A
  # leading "v" is stripped: rpm and AppStream both want a bare number.
  TAG: ${{ github.event.release.tag_name || inputs.tag }}

jobs:
  packages:
    # One build of Stud, wrapped four ways. Building each format in its
    # own job would mean four builds of ANGLE -- an hour each -- and no
    # guarantee that the packages hold the same binaries.
    name: rpm, deb, Arch, cpak image
    runs-on: ubuntu-latest
    container: fedora:42
    steps:
      - name: Install build dependencies
        # Before checkout on purpose: the image has no git, and
        # actions/checkout and actions/cache both need it and tar.
        run: |
          dnf install -y --setopt=install_weak_deps=False \
            gcc-c++ cmake ninja-build git pkgconf-pkg-config rpm-build \
            qt6-qtbase-devel qt6-qtwebengine-devel qtkeychain-qt6-devel \
            wayland-devel wayland-protocols-devel libxkbcommon-devel \
            libglvnd-devel vulkan-loader-devel vulkan-headers \
            freetype-devel portaudio-devel openssl-devel \
            curl unzip tar xz zstd python3 e2fsprogs findutils

      - uses: actions/checkout@v7
        with:
          # The tag being released, not whatever the default branch holds:
          # the packages attached to a release must be built from the code
          # that release names.
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - name: Free up disk space
        # Only matters on a cache miss, when ANGLE's own checkout needs
        # about 15 GB. Harmless otherwise.
        run: rm -rf /__t/CodeQL /__t/Java_Temurin-Hotspot_jdk /__t/Ruby /__t/go /__t/node || true

      - name: Restore the Android NDK
        # One key for both jobs: the NDK is downloaded, not built, so the
        # container that extracted it makes no difference -- it compiles
        # Process B with its own toolchain rather than the host's. Caching
        # it per-container stored 2.2 GB twice against a 10 GB limit and
        # made the AppImage job re-download what this one had just fetched.
        uses: actions/cache@v6
        with:
          path: third_party/android-ndk-r28c
          key: ndk-${{ hashFiles('tools/setup.sh') }}

      - name: Restore ANGLE and bionic
        # Per-container, unlike the NDK above: ANGLE is COMPILED here,
        # against this container's glibc, so a Fedora build and an Ubuntu
        # one are not interchangeable.
        uses: actions/cache@v6
        with:
          path: |
            third_party/angle
            third_party/android-bionic
          key: deps-fedora42-${{ hashFiles('tools/setup.sh') }}

      - name: Fetch dependencies
        # A cold cache builds ANGLE from source: several GB of checkout and
        # about an hour. Its sources are no use afterwards -- only the two
        # libraries it produced, which is all the cache keeps -- so they go
        # straight back to the disk the rest of the build needs.
        run: |
          df -h /__w | tail -1
          tools/setup.sh
          rm -rf third_party/angle-src third_party/depot_tools
          df -h /__w | tail -1

      - name: Build
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DSTUD_VERSION="${TAG#v}"
          cmake --build build -j"$(nproc)"

      # One build, several wrappers. Building each format separately would
      # mean several builds of ANGLE and no guarantee the packages contain
      # the same binaries -- so the install tree is produced once here and
      # every package is made from it.
      - name: Package the rpm and the deb
        run: cd build && cpack -G RPM && cpack -G DEB

      - name: Lay out the install tree the rest of the packages come from
        run: |
          DESTDIR="$PWD/install-tree" cmake --install build
          # What the Flatpak manifest expects: the tree rooted at usr/,
          # compressed the way its `type: archive` source reads it.
          tar -C install-tree -caf "stud-${TAG#v}-x86_64.tar.zst" usr

      - uses: actions/upload-artifact@v7
        with:
          name: packages
          path: |
            build/*.rpm
            build/*.deb
            *.pkg.tar.zst
            stud-*-x86_64.tar.zst
          if-no-files-found: error

  # Arch package and cpak image.
  #
  # These run HERE, on the runner, and not in the packages job -- that one
  # runs inside a Fedora container, and podman is installed on the runner,
  # not in the container. It failed there with `podman: command not found`
  # (exit 127) and, because both steps carried continue-on-error, the job
  # still went green while producing neither. Two of five package formats
  # were silently missing from the release.
  containers:
    name: Arch package and cpak image
    needs: packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      TAG: ${{ github.event.release.tag_name || inputs.tag }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - uses: actions/download-artifact@v8
        with:
          name: packages
          path: artifacts

      - name: Unpack the install tree
        # stud-<version>-x86_64.tar.zst IS the install tree, rooted at
        # usr/ -- the same archive the Flatpak and stud-bin install. No
        # second copy is made for this.
        run: |
          mkdir -p install-tree
          archive="$(find artifacts -name 'stud-*-x86_64.tar.zst' | head -1)"
          [ -n "$archive" ] || { echo "no install tree in the artifacts" >&2; exit 1; }
          tar -C install-tree -xf "$archive"
          test -d install-tree/usr

      - name: Package the Arch pkg.tar.zst
        # makepkg is Arch's and refuses to run as root, hence the
        # unprivileged user. It compiles nothing: the PKGBUILD copies the
        # install tree that already exists.
        run: |
          cp packaging/aur/PKGBUILD.bin "$PWD/PKGBUILD"
          cat > /tmp/makepkg.sh <<'SCRIPT'
          set -eux
          pacman -Sy --noconfirm --needed base-devel zstd
          useradd -m builder
          cp /work/PKGBUILD /home/builder/
          chown -R builder:builder /home/builder
          su builder -c "cd /home/builder && STUD_DESTDIR=/work/install-tree PKGEXT=.pkg.tar.zst makepkg --nodeps --noconfirm"
          cp /home/builder/*.pkg.tar.zst /work/
          SCRIPT
          chmod +x /tmp/makepkg.sh
          podman run --rm -v "$PWD":/work:z -v /tmp/makepkg.sh:/makepkg.sh:z \
            docker.io/library/archlinux:base-devel /makepkg.sh
          # No continue-on-error: a missing package should fail the job,
          # not vanish quietly.
          ls -la ./*.pkg.tar.zst

      - name: Push the cpak image to ghcr.io
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${TAG#v}"
          podman build -f packaging/cpak/Containerfile.prebuilt \
            -t "ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/stud:${version}" \
            -t "ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/stud:latest" .
          echo "$GH_TOKEN" | podman login ghcr.io -u "${GITHUB_ACTOR}" --password-stdin
          podman push "ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/stud:${version}"
          podman push "ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/stud:latest"
          podman inspect --format '{{index .RepoDigests 0}}' \
            "ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/stud:${version}" | tee cpak-image-digest.txt

      - uses: actions/upload-artifact@v7
        with:
          name: containers
          path: |
            *.pkg.tar.zst
            cpak-image-digest.txt
          if-no-files-found: error

  appimage:
    name: AppImage
    runs-on: ubuntu-latest
    # The same base packaging/Containerfile.appimage pins, for the same
    # reasons: Qt 6.8 and a KF6 that has the real Breeze style. Running it
    # as the job's container means build-appimage.sh does not have to
    # start one of its own.
    container: ubuntu:25.04
    env:
      STUD_APPIMAGE_IN_CONTAINER: "1"
      APPIMAGE_EXTRACT_AND_RUN: "1"
      DEBIAN_FRONTEND: noninteractive
    steps:
      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build pkg-config git curl ca-certificates \
            file desktop-file-utils zsync fuse3 unzip tar xz-utils zstd python3 e2fsprogs \
            qt6-base-dev qt6-base-dev-tools qt6-webengine-dev qt6-webengine-dev-tools \
            qt6-l10n-tools libqt6opengl6-dev qtkeychain-qt6-dev libssl-dev \
            qt6-wayland qt6-wayland-dev qt6-xdgdesktopportal-platformtheme \
            kde-style-breeze \
            libwayland-dev libwayland-egl-backend-dev wayland-protocols \
            libegl1-mesa-dev libgles2-mesa-dev libfreetype-dev libvulkan-dev \
            libxkbcommon-dev portaudio19-dev

      - uses: actions/checkout@v7
        with:
          # The tag being released, not whatever the default branch holds:
          # the packages attached to a release must be built from the code
          # that release names.
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - name: Free up disk space
        run: rm -rf /__t/CodeQL /__t/Java_Temurin-Hotspot_jdk /__t/Ruby /__t/go /__t/node || true

      - name: Restore the Android NDK
        # The same key the packages job uses -- see the comment there.
        uses: actions/cache@v6
        with:
          path: third_party/android-ndk-r28c
          key: ndk-${{ hashFiles('tools/setup.sh') }}

      - name: Restore ANGLE and bionic
        uses: actions/cache@v6
        with:
          path: |
            third_party/angle
            third_party/android-bionic
          key: deps-ubuntu2504-${{ hashFiles('tools/setup.sh') }}

      - name: Restore linuxdeploy and appimagetool
        uses: actions/cache@v6
        with:
          path: third_party/appimage-tools
          key: appimage-tools-${{ hashFiles('packaging/build-appimage.sh') }}

      - name: Fetch dependencies
        # A cold cache builds ANGLE from source: several GB of checkout and
        # about an hour. Its sources are no use afterwards -- only the two
        # libraries it produced, which is all the cache keeps -- so they go
        # straight back to the disk the rest of the build needs.
        run: |
          df -h /__w | tail -1
          tools/setup.sh
          rm -rf third_party/angle-src third_party/depot_tools
          df -h /__w | tail -1

      - name: Build the AppImage
        run: |
          export STUD_CMAKE_ARGS="-DSTUD_VERSION=${TAG#v}"
          packaging/build-appimage.sh

      - uses: actions/upload-artifact@v7
        with:
          name: appimage
          path: build-appimage/packages/*.AppImage
          if-no-files-found: error

  flatpak:
    # After the job above, because it installs the archive that job built.
    name: Flatpak bundle (needs the build above)
    needs: packages
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:kde-6.10
      options: --privileged
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - uses: actions/download-artifact@v8
        with:
          name: packages
          path: artifacts

      - name: Point the manifest at the archive we just built
        # The manifest in the tree names the release URL, which does not
        # exist until this release is published -- so for the bundle it is
        # rewritten to the local file. Same archive either way; only where
        # flatpak-builder reads it from differs.
        run: |
          archive="$(find artifacts -name 'stud-*-x86_64.tar.zst' | head -1)"
          [ -n "$archive" ] || { echo "no flatpak archive in the artifacts" >&2; exit 1; }
          cp "$archive" stud-archive.tar.zst
          python3 - <<'PY'
          import re
          p = "packaging/flatpak/io.github.catpieleaf.Stud.yml"
          s = open(p).read()
          s = re.sub(r"        url: https://github\.com/CatPieLeaf/Stud/releases/[^\n]*\n"
                     r"        sha256: PLACEHOLDER_STUD_SHA256\n",
                     "        path: ../../stud-archive.tar.zst\n", s)
          open(p, "w").write(s)
          PY
          grep -n "path: \|url: " packaging/flatpak/io.github.catpieleaf.Stud.yml

      - name: The manifest must carry no placeholder into the build
        # The dependency checksums are real in the tree -- Flathub builds
        # from the manifest as committed, so a manifest that only works
        # after CI has rewritten it is not one they can build at all. The
        # only substitution left is the archive path above.
        run: |
          ! grep -q PLACEHOLDER packaging/flatpak/io.github.catpieleaf.Stud.yml

      - name: Build and bundle
        run: |
          flatpak-builder --force-clean --disable-rofiles-fuse \
            --repo=flatpak-repo build-dir \
            packaging/flatpak/io.github.catpieleaf.Stud.yml
          flatpak build-bundle flatpak-repo \
            "stud-${TAG#v}-x86_64.flatpak" io.github.catpieleaf.Stud

      - uses: actions/upload-artifact@v7
        with:
          name: flatpak-bundle
          path: "*.flatpak"
          if-no-files-found: error

  publish:
    name: Attach every package to the release
    needs: [packages, containers, appimage, flatpak]
    runs-on: ubuntu-latest
    # Not in a container: this is where the GitHub CLI already lives.
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Upload to the release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          find artifacts -type f -printf '%f  %s bytes\n'
          # Found rather than assumed: the artifact actions have changed
          # where they unpack to before, and a release that silently gets
          # no packages attached is worse than a loud failure.
          mapfile -t packages < <(find artifacts -type f \
            \( -name '*.rpm' -o -name '*.deb' -o -name '*.AppImage' \
               -o -name '*.pkg.tar.zst' -o -name 'stud-*-x86_64.tar.zst' \
               -o -name '*.flatpak' \))
          # rpm, deb, AppImage and the Flatpak archive are required. The
          # Arch package is not: its job is allowed to fail, since makepkg
          # in a container is the one step here that depends on something
          # outside this repository's control.
          for required in '\.rpm$' '\.deb$' '\.AppImage$' '\.flatpak$' '\.pkg\.tar\.zst$'; do
            printf '%s\n' "${packages[@]}" | grep -qE "$required" ||
              { echo "no package matching $required" >&2; exit 1; }
          done
          gh release upload "$TAG" "${packages[@]}" --clobber

      # The checksums the AUR PKGBUILD and the Flatpak manifest need. Both
      # carry a placeholder in the tree on purpose -- a checksum belongs to
      # one specific archive, and a stale one is worse than an obvious gap
      # -- so the values are printed here, where whoever updates those two
      # repositories can copy them.
      - name: Checksums for the AUR and Flathub repositories
        run: |
          echo "Source tarball (AUR sha256sums):"
          curl -fsSL "https://github.com/${{ github.repository }}/archive/refs/tags/${TAG}.tar.gz" \
            | sha256sum | cut -d' ' -f1
          echo
          echo "Flatpak archive (manifest sha256):"
          find artifacts -name 'stud-*-x86_64.tar.zst' -exec sha256sum {} + | cut -d' ' -f1
